name: openrouter
version: 1.0.0
description: "OpenRouter image generation backend (DALL-E 3, Flux, etc.) via chat completions API."
author: Hermes Agent Community
kind: backend
requires_env:
  - OPENROUTER_API_KEY
