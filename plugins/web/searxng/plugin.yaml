name: web-searxng
version: 1.0.0
description: "SearXNG web search + extraction — free, self-hosted, privacy-respecting metasearch engine. Requires SEARXNG_URL pointing at your instance. Supports search and URL content extraction via fetch_url parameter."
author: NousResearch
kind: backend
provides_web_providers:
  - searxng
capabilities:
  - search
  - extract
